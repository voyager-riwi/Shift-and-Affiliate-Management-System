@{
    // Usamos el layout principal aquí, ya que es una página administrativa/navegación.
    ViewData["Title"] = "Menú Principal - System EPS";
}

<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;800&display=swap" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

<style>
    .card-link {
        transition: all 0.3s ease;
        text-decoration: none;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }
    .card-link:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.2), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    .icon-box {
        width: 4rem;
        height: 4rem;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 0.75rem;
    }
</style>

<div class="container mx-auto p-4 md:p-10">
    <div class="text-center mb-12">
        <h1 class="text-5xl font-extrabold text-gray-900 mb-2">System EPS</h1>
        <p class="text-xl text-gray-600">Bienvenido al Centro de Control de Turnos de Salud Linux</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        
        <!-- Módulo 1: Kiosko (Acceso Cliente) -->
        <a href="@Url.Action("Kiosk", "Home")" class="card-link block bg-white border border-gray-200 rounded-xl p-6 text-gray-800">
            <div class="flex flex-col items-center text-center h-full">
                <div class="icon-box bg-blue-100 text-blue-600 mb-4">
                    <i class="fas fa-tablet-alt fa-2x"></i>
                </div>
                <h2 class="text-2xl font-bold mb-2">Kiosko de Turnos</h2>
                <p class="text-gray-500 flex-grow">Pantalla táctil para que los afiliados generen su tiquete rápidamente. Ideal para uso público.</p>
                <span class="mt-4 text-sm font-semibold text-blue-600 hover:text-blue-700">Ir al Kiosko →</span>
            </div>
        </a>

        <!-- Módulo 2: Dashboard (Acceso Operador) -->
        <a href="@Url.Action("Dashboard", "Home")" class="card-link block bg-white border border-gray-200 rounded-xl p-6 text-gray-800">
            <div class="flex flex-col items-center text-center h-full">
                <div class="icon-box bg-green-100 text-green-600 mb-4">
                    <i class="fas fa-desktop fa-2x"></i>
                </div>
                <h2 class="text-2xl font-bold mb-2">Panel de Control</h2>
                <p class="text-gray-500 flex-grow">Herramienta para el personal de servicio. Permite llamar turnos y ver la cola de espera.</p>
                <span class="mt-4 text-sm font-semibold text-green-600 hover:text-green-700">Abrir Panel de Control →</span>
            </div>
        </a>

        <!-- Módulo 3: Turnero (Acceso Público) -->
        <a href="@Url.Action("TurnDisplay", "Home")" target="_blank" class="card-link block bg-white border border-gray-200 rounded-xl p-6 text-gray-800">
            <div class="flex flex-col items-center text-center h-full">
                <div class="icon-box bg-red-100 text-red-600 mb-4">
                    <i class="fas fa-tv fa-2x"></i>
                </div>
                <h2 class="text-2xl font-bold mb-2">Pantalla de Llamadas</h2>
                <p class="text-gray-500 flex-grow">Visualización a gran escala de los turnos activos. Diseñado para mostrar en monitores grandes.</p>
                <span class="mt-4 text-sm font-semibold text-red-600 hover:text-red-700">Ver Turnero Digital →</span>
            </div>
        </a>
    </div>

    <div class="text-center mt-12 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">Desarrollado para la gestión eficiente de la atención en Salud Linux.</p>
    </div>
</div>
